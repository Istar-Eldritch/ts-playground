version: '2'
services:
  play:
    command: /bin/sh -c "npm run setup && npm run start"
    image: node
    working_dir: /opt
    volumes:
     - .:/opt
     - /opt/node_modules
     - /opt/typings
    environment:
      TERM: dumb
