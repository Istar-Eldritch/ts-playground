{
  "name": "ts-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "setup": "npm i && typings i",
    "start": "nodemon -e ts -q -x 'clear && rm -fr dist && tsc --outDir dist && node dist/main/index.js'",
    "test": "tape dist/test/**/*.spec.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.10.0",
    "tape": "^4.6.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
